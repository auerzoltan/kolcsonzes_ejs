<%- include('header') %>

<h4 class="mb-3">Rents</h4>

<%- include('message') %>

<table class="table table-striped table-hover">
    <thead>
        <tr>
            <th>#</th>
            <th>Title</th>
            <th>Rented</th>
            <th class="text-end">Returned</th>
        </tr>
    </thead>
    <tbody>
        <% rents.forEach((item, index) => { %>
            <tr>    
                <td><%= (index+1) %>.</td>
                <td><%= item.title %></td>
                <td><%= item.start %></td>
                <td class="text-end"><%= item.end %></td>
            </tr>

        <% }); %>
    </tbody>
</table>

<%- include('footer') %>